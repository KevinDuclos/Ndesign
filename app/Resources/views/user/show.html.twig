{% extends 'base_admin.html.twig' %}

{% block body %}
<div class="col-sm-9 col-sm-offset-3 col-lg-10 col-lg-offset-2 main">	
    <h1>User</h1>

    <table>
        <tbody>
            <tr>
                <th>Utilisateur</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>E-mail</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Date de création</th>
                <td>{% if user.createdat %}{{ user.createdat|date('Y-m-d') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('app_users') }}">Retour à la liste des utilisateurs</a>
        </li>
        <li>
            <a href="{{ path('user_edit', { 'id': user.id }) }}">Editer l'utilisateur</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer cet utilisateur">
            {{ form_end(delete_form) }}
        </li>
    </ul>
</div>
{% endblock %}
