{% extends 'base_admin.html.twig' %}

{% block body %}
    <h1>Edition de produit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form,  { "class" : "form-control"}) }}
        <input type="submit" value="Editer le produit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('produit_index') }}">Retour à la liste des produits</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer ce produit">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
